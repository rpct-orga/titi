name: Actions Runner Controller Demo
on:
  workflow_dispatch:

jobs:
  container-job:
    runs-on: runner-test-02
    container: node:17.6.0
    steps:
      - run: node --version
      - run: npm --version
